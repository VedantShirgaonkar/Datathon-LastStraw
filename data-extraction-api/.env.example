# ===========================================
# Data Extraction API - Environment Variables
# ===========================================
# Copy this file to .env and fill in your credentials

# -------------------------------------------
# Webhook Secrets (optional but recommended)
# -------------------------------------------
# GitHub webhook secret for HMAC-SHA256 signature verification
# Set this in GitHub repo → Settings → Webhooks → Secret
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret

# Jira webhook secret for signature verification (optional)
JIRA_WEBHOOK_SECRET=your-jira-webhook-secret

# -------------------------------------------
# Notion Configuration
# -------------------------------------------
# Notion Integration Token (create at: https://www.notion.so/my-integrations)
NOTION_TOKEN=secret_your_notion_token

# Notion Database ID to query
# (found in database URL: https://notion.so/{workspace}/{database_id}?v=...)
NOTION_DATABASE_ID=your-database-id

# -------------------------------------------
# Optional: HTTP Client Configuration
# -------------------------------------------
# Request timeout in seconds (default: 10)
# REQUEST_TIMEOUT=10

# Maximum retry attempts for failed requests (default: 3)
# MAX_RETRIES=3
