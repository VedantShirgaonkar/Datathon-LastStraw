# Core Dependencies
neo4j
python-dotenv>=1.0.0

# Webhook Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# AI Agent Dependencies
langchain
langchain-openai  # Featherless AI uses OpenAI-compatible API
langgraph
langchain-community
pydantic
pydantic-settings

# Database drivers
psycopg2-binary  # PostgreSQL + pgvector
clickhouse-connect  # ClickHouse Cloud
pgvector  # pgvector Python client for vector storage and search
pinecone[grpc]  # Pinecone inference API for embeddings

# Kafka/Streaming (optional - only if using Kafka instead of webhooks)
#kafka-python  # Apache Kafka consumer

# AWS SDK (for Lambda executor)
boto3  # AWS Lambda invocation for executor commands

# HTTP client (for testing webhooks)
requests

# Development dependencies (uncomment if needed)
# pytest==8.3.5
# black==25.1.0
# ruff==0.8.6
