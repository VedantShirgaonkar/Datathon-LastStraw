from setuptools import setup, find_packages

setup(
    name="hr-voice-agent",
    version="0.1.0",
    description="Personalized monthly HR review voice agent (FastAPI)",
    packages=find_packages(),
    python_requires=">=3.9",
)
